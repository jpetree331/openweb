[project]
name = "openwebui"
version = "0.1.0"
description = "OpenWebUI project managed with uv"
requires-python = ">=3.11,<3.13"
dependencies = [
    "open-webui",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
